apiVersion: apps/v1
kind: Deployment
metadata:
  name: ai-recommendation-service
  namespace: tasktracker
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ai-recommendation-service
  template:
    metadata:
      labels:
        app: ai-recommendation-service
    spec:
      initContainers:
        - name: wait-for-eureka
          image: curlimages/curl:latest
          command:
            - sh
            - -c
            - 'until curl -s http://eureka-server:8761/eureka/apps > /dev/null; do echo "Waiting for Eureka Server..."; sleep 5; done'
      containers:
        - name: ai-recommendation-service
          imagePullPolicy: Always
          image: ghcr.io/fl1s/tasktracker/ai-recommendation-service:latest
          ports:
            - containerPort: 8082
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "prod"
      imagePullSecrets:
        - name: ghcr-secret
---
apiVersion: v1
kind: Service
metadata:
  name: ai-recommendation-service
  namespace: tasktracker
spec:
  selector:
    app: ai-recommendation-service
  ports:
    - port: 8082
      targetPort: 8082
  type: NodePort
