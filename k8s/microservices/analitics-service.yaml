apiVersion: apps/v1
kind: Deployment
metadata:
  name: analitics-service
  namespace: tasktracker
spec:
  replicas: 1
  selector:
    matchLabels:
      app: analitics-service
  template:
    metadata:
      labels:
        app: analitics-service
    spec:
      initContainers:
        - name: wait-for-eureka
          image: curlimages/curl:latest
          command:
            - sh
            - -c
            - 'until curl -s http://eureka-server:8761/eureka/apps > /dev/null; do echo "Waiting for Eureka Server..."; sleep 5; done'
      containers:
        - name: api-gateway
          imagePullPolicy: Always
          image: ghcr.io/fl1s/tasktracker/analitics-service:latest
          ports:
            - containerPort: 8083
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "prod"
      imagePullSecrets:
        - name: ghcr-secret
---
apiVersion: v1
kind: Service
metadata:
  name: analitics-service
  namespace: tasktracker
spec:
  selector:
    app: analitics-service
  ports:
    - port: 8083
      targetPort: 8083
  type: NodePort
